[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "home-cli"
version = "1.0.0"
dependencies = ["requests"]

[project.scripts]
home = "cli.main:main"

[tool.setuptools]
packages = ["cli"]